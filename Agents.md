create requirements and then design and tasks document, all the things  in the task must and should use execute from tasks.md only
each time we need to update the tasks.md after each successfull completion

we need to use code completion graph and update graph index sequence each time,
use any best code graph tool of your choice

for epaper-extract project we should use specs/epaper-extract folder only

for epaper-extract project implementaion we should use the epaper-extract in root folder.

